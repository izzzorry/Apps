import { FotosPersonajesPipe } from './fotos-personajes.pipe';

describe('FotosPersonajesPipe', () => {
  it('create an instance', () => {
    const pipe = new FotosPersonajesPipe();
    expect(pipe).toBeTruthy();
  });
});
